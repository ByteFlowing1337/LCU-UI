{
  "name": "lcu-ui-tauri",
  "version": "0.1.0",
  "description": "Tauri shell for LCU UI Flask App",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "build:backend": "powershell -ExecutionPolicy Bypass -File build_backend.ps1",
    "build": "npm run build:backend && tauri build"
  },
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  },
  "dependencies": {}
}
